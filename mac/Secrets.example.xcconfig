// The relay endpoint (host / port / TLS / password) is no longer a build setting.
// Configure it at runtime in the app's "Server Settings…" window instead — those values are
// persisted (UserDefaults + Keychain) and embedded in the pairing QR for the phone.
//
// This file is kept only as an optional, gitignored override hook for future build settings.
// You can leave it empty or delete your Secrets.xcconfig entirely.
