<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>LinkToMac</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<!-- Menubar agent: no Dock icon, no main window. -->
	<key>LSUIElement</key>
	<true/>
	<!-- Bluetooth: scan for the phone's presence beacon to auto-lock when it leaves. -->
	<key>NSBluetoothAlwaysUsageDescription</key>
	<string>LinkToMac scans for your paired phone over Bluetooth to lock this Mac when you walk away.</string>
	<!-- Allow cleartext ws:// to localhost / local network for the dev relay. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- Relay endpoint, substituted from build settings (Config.xcconfig / Secrets.xcconfig)
	     at build time and read by Config.swift. Keeps the host + token out of tracked source. -->
	<key>RelayHost</key>
	<string>$(RELAY_HOST)</string>
	<key>RelayPort</key>
	<string>$(RELAY_PORT)</string>
	<key>RelayAuthToken</key>
	<string>$(RELAY_AUTH_TOKEN)</string>
</dict>
</plist>
