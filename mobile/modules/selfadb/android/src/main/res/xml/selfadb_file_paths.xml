<?xml version="1.0" encoding="utf-8"?>
<!-- Exposes ONLY cacheDir/clips (received clipboard images) through the selfadb FileProvider. -->
<paths>
    <cache-path name="clips" path="clips/" />
</paths>
